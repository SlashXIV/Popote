<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:RecettesApp.Views"
       x:Class="RecettesApp.AppShell">

    <!-- Page d'accueil = la liste des recettes -->
    <ShellContent Title="Recettes"
                  ContentTemplate="{DataTemplate views:RecipeListPage}"
                  Route="RecipeListPage" />

</Shell>
